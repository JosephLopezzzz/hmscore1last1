// Test file to verify modal imports
import {
  BillingModal,
  CheckInOutModal,
  FeedbackModal,
  HousekeepingModal,
  LoyaltyProgramModal,
  MaintenanceModal,
  PaymentsModal,
  ReservationModal
} from './index';

// This will show TypeScript errors if any imports are invalid
const testImports = {
  BillingModal,
  CheckInOutModal,
  FeedbackModal,
  HousekeepingModal,
  LoyaltyProgramModal,
  MaintenanceModal,
  PaymentsModal,
  ReservationModal
};

console.log('All modal imports are valid:', testImports);
